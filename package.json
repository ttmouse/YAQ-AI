{
  "name": "yaq-ai",
  "version": "1.0.0",
  "private": true,
  "description": "YAQ-AI: 安全监管智能工作台",
  "scripts": {
    "build": "bash build.sh",
    "build:js": "npx terser js/app.js -o dist/js/app.js --compress --mangle --comments false && npx terser js/track-store.js -o dist/js/track-store.js --compress --mangle --comments false && npx terser js/rules.js -o dist/js/rules.js --compress --mangle --comments false && npx terser js/agent-init.js -o dist/js/agent-init.js --compress --mangle --comments false",
    "build:html": "cp index.html dist/ && cp 404.html dist/ && cp ai-vs-traditional-comparison.html dist/ && cp special-inspection-prototype.html dist/ && cp favicon.svg dist/ && cp .nojekyll dist/ && cp robots.txt dist/",
    "deploy": "echo '部署通过 GitHub Actions 自动完成 (git push → deploy-pages.yml)'",
    "lint": "eslint js/",
    "lint:fix": "eslint js/ --fix",
    "format": "prettier --check 'js/**/*.js' 'css/**/*.css' '*.html'",
    "format:fix": "prettier --write 'js/**/*.js' 'css/**/*.css' '*.html'",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run lint && npm run test"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "clean-css-cli": "^5.6.3",
    "eslint": "^10.5.0",
    "globals": "^17.7.0",
    "jsdom": "^29.1.1",
    "prettier": "^3.8.4",
    "terser": "^5.31.0",
    "vitest": "^4.1.9"
  }
}
