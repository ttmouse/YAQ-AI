{
  "name": "yaq-ai",
  "version": "1.0.0",
  "private": true,
  "description": "YAQ-AI: 安全监管智能工作台",
  "scripts": {
    "build": "bash build.sh",
    "build:js": "npx terser js/app.js -o dist/js/app.js --compress --mangle --comments false && npx terser js/rules.js -o dist/js/rules.js --compress --mangle --comments false && npx terser js/agent-init.js -o dist/js/agent-init.js --compress --mangle --comments false",
    "build:html": "cp index.html dist/ && cp 404.html dist/ && cp ai-vs-traditional-comparison.html dist/ && cp special-inspection-prototype.html dist/ && cp favicon.svg dist/ && cp .nojekyll dist/ && cp robots.txt dist/",
    "deploy": "echo '部署通过 GitHub Actions 自动完成 (git push → deploy-pages.yml)'"
  },
  "devDependencies": {
    "terser": "^5.31.0",
    "clean-css-cli": "^5.6.3"
  }
}
